
constroller {

    try {
        const ids = service.getIdFromCsv()

    } catch (error) {
        catchError(error)
    }

    Abrir el archivo csv 
    Consumir el endpoint de la imagen
    licensiar imagen
    guardar imagen

}